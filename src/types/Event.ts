// src/types/Event.ts
export interface Event {
    id: string; // Adjust the type based on your data
    title: string;
    description: string;
    time: number; // Assuming duration is a number
    image: string; // Assuming this is the image path or URL
  }
  